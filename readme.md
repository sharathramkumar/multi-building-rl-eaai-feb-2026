Installation steps:

1. In a fresh Python (Python >=3.10) environment, run the following commands:
    - `pip install torch --index-url https://download.pytorch.org/whl/cpu`
    - `pip install -e .` 

The first step can be avoided if you need to install a GPU-enabled version of Pytorch.
